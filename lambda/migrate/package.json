{
  "name": "migrate-lambda",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc && cp -r ../../packages/api/migrations dist/migrations",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/pg": "^8.11.0",
    "typescript": "^5.4.5"
  }
}
